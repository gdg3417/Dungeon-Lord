# Sprint 2 to Sprint 4 Test-Stage Matrix

**Current Phase 2B6B working status (2026-08-09):** Schema-7 serialized ownership, canonical graph/content runtime projection, canonical-mode legacy-normalization suppression, and all registry-owned Bootstrap English localization mappings are now implemented with focused contracts. Live activation remains intentionally blocked: the detached raw classifier, canonical serializer, whole-save candidate, transaction, and recovery APIs require caller-supplied save-byte, scan-work, JSON-node/record, unknown-member, and diagnostic workload limits, but production owns only the five spatial-content limits in `Assets/_Project/Data/Production/DungeonSpatial/validation_limits.json`. Locked Specs 19, 36, and 38 explicitly say those values are not schema/save limits and prohibit a compiled or test-default fallback. No approved production config owns the missing save workload values, so inventing them would violate the activation packet. `SaveMigration.LatestSchemaVersion` therefore remains **6**, raw transaction integration and canonical writers remain inactive, and GD66 is not complete. Owner approval and configuration ownership for those technical limits are required before schema 7 can be safely activated. PR #195 remains the single final Phase 2B6B working PR; this incomplete state is not a separately mergeable readiness phase.

**Current PR #195 fixture ownership (incomplete acceptance coverage):** `Gd66CanonicalRuntimeProjectionTests` uses a context-validated production/profile-derived migration fixture to cover canonical route/content projection and tampered-legacy immunity, and adds malformed canonical-looking-state, contextual-publication, normalization-suppression, and fail-closed legacy-writer coverage. `Gd66MigrationLocalizationTests` derives the complete player-key set from `Gd66MigrationReasonRegistry` and requires all 87 exact mappings in Bootstrap English. These fixtures are incomplete working-PR coverage only: live raw interception, migration/recovery integration, native creation, atomic canonical mutation, and schema 7 remain blocked by the missing production save-workload configuration authority described above. Final activation coverage must also prove that complete validated bytes or equivalent extension evidence survives ordinary canonical mutations so unknown root and primary members are not lost.

**Historical Phase 2B6A status:** PR #194 is merged at `2bcc336f5fbbb9797f6f319f738e7b9f7d0613bd`; detached candidate/transaction/recovery and Windows durability qualification tests are authoritative. Phase 2B6A adds selector/preflight coverage and Windows-only integration coverage for durable create/readback, replacement, quarantine moves, duplicate-evidence collapse, and same-directory rejection. When the one deterministic quarantine already contains the exact evidence, the source is collapsed into that same artifact by verified same-directory replacement, flush, source-absence check, and quarantine readback; no second evidence namespace is created. Real filesystem tests are explicitly gated to Windows Editor; a non-Windows skip is not qualification evidence. Supported qualification scope is Windows Editor and Windows Standalone on local, nonredirected NTFS; all other targets and filesystem conditions fail closed. The PR #194 Unity and Windows Standalone durability qualification is complete; schema-7 lifecycle validation remains owner-run after activation. Schema remains **6**, schema 7 remains inactive, all normal save/runtime authorities remain unchanged, Phase 2B6B owns activation, and GD66 is not complete.

**Phase 2B6A fixture ownership:** `Gd66WindowsSpatialMigrationFileSystemTests` owns one exact emitting fixture for every registered `gd66.preflight.*` result, handle-based `SetFileInformationByHandle(FileRenameInfo)` move/replace and static no-`MoveFileExW` verification, verified duplicate-quarantine replacement, create-new plus locked-source/destination native failures and cleanup/retry, same-directory enforcement, and Windows-Editor real-filesystem transaction qualification including persisted `Replaced` with and without B. `Gd66DetachedSpatialMigrationTransactionTests` and `Gd66DetachedSpatialMigrationTransactionFailureMatrixTests` retain Packet C recovery/stage/failure ownership. Windows-only preflight failures are assertions, never qualification skips; Windows Standalone remained a distinct owner-run player qualification and passed for PR #194 rather than being inferred from Editor coverage.

**Windows Standalone player qualification route:** `Gd66WindowsStandaloneQualificationTests` is a PlayMode test assembly constrained by `UNITY_INCLUDE_TESTS` and `WindowsStandalone64`; it is test-runner code and is excluded from ordinary release players. After creating `artifacts/`, the owner runs exactly `Unity -batchmode -projectPath . -runTests -testPlatform StandaloneWindows64 -testFilter DungeonBuilder.M0.Tests.PlayMode.Gd66WindowsStandaloneQualificationTests -testResults artifacts/gd66-windows-standalone.xml -logFile artifacts/gd66-windows-standalone.log`. `-testPlatform PlayMode -buildTarget StandaloneWindows64` is not equivalent: it executes PlayMode tests in the Editor, where this fixture emits the `gd66.test.windows_player_only` skip. For GUI execution in Unity `6000.3.2f1`, the owner switches the active build target to Windows x86_64, opens Test Runner, selects `DungeonBuilder.M0.Tests.PlayMode.Gd66WindowsStandaloneQualificationTests`, and uses the Test Runner control that runs the selected PlayMode test in a player rather than the normal Editor PlayMode run. This document intentionally does not assign that control an unverified menu label; the repository pins Test Framework `1.6.0`, but its installed UI/package source is not present in this workspace to verify localized UI wording. The owner records the exact displayed control wording in the evidence log.

The test player must execute with `Application.platform == RuntimePlatform.WindowsPlayer` and report the fixture **passed**, not ignored or skipped. An Editor PlayMode pass and any result containing `gd66.test.windows_player_only` as a skip do not qualify Windows Standalone. The test uses `Application.temporaryCachePath/gd66-windows-standalone-qualification`, invokes the public runtime selector and production filesystem, and qualifies only Windows-player preflight and native filesystem operations; full transaction, candidate-authority, and recovery qualification remains owned by the Windows Editor tests. Evidence is both `artifacts/gd66-windows-standalone.xml` and `artifacts/gd66-windows-standalone.log`, and both must identify the reviewed Git commit. A failed player build, undiscovered fixture, failed/ignored/skipped result, missing XML, or missing player/editor log is a failed qualification attempt. That evidence passed at the PR #194 reviewed head; it does not qualify later schema-7 activation changes. Schema remains 6, schema 7 remains inactive, and ordinary gameplay invokes neither detached migration nor recovery.

The qualification assembly includes exactly `Editor` and `WindowsStandalone64`: Editor inclusion permits Unity Test Framework to discover and select the fixture before constructing a player run, while `UNITY_INCLUDE_TESTS` plus `TestAssemblies` keeps it out of ordinary production players. Direct Editor PlayMode execution intentionally ignores the fixture with `gd66.test.windows_player_only`; only execution inside `RuntimePlatform.WindowsPlayer` qualifies. A zero-test XML is a failed qualification attempt even when its root result says `Passed`. Required standalone evidence executes exactly `DungeonBuilder.M0.Tests.PlayMode.Gd66WindowsStandaloneQualificationTests.WindowsStandalonePreflightAndNativeFilesystemQualification` and reports `testcasecount="1"`, `total="1"`, `passed="1"`, `failed="0"`, and `skipped="0"`; the log must not report `No tests were executed`, and neither artifact may contain the player-only ignore reason. Windows Editor suites may contain the two intentional ignores `gd66.test.windows_only_inverse` and `gd66.test.windows_player_only`; neither is standalone evidence.

Unity Test Framework Windows player builds supply exactly one generated `Assets/InitTestScene<valid-guid>.unity` before canonical `Assets/_Project/Scenes/Bootstrap.unity`. The Editor build preprocessor recognizes that infrastructure only when `BuildOptions.IncludeTestAssemblies` is set, the target is exactly `StandaloneWindows64`, the two scenes occur in that exact ordinal order, and the generated identity parses as a GUID. It then passes only canonical Bootstrap to the unchanged production scene-composition authority while still requiring production recovery, installed spatial-content validation, and Bootstrap assignment/composition validation. Normal player builds remain exactly Bootstrap-only. Missing flags, another target, malformed or non-Assets initialization paths, similar names, missing/reversed/duplicate/extra scenes, and every other multi-scene build remain `InvalidBuildSceneComposition`; this is not a general multi-scene exception. The owner must rerun the standalone route after this preprocessor correction before qualification may be claimed.


**GD65B5 final status:** Implementation and required owner validation passed at `c5eefae61e9bf3b7bf0a200e343f383f0122743b` in PR #186. PR #186 is merged; GD65B is closed and GD66 was subsequently approved in merged PR #187. The production spatial catalog remains inactive, existing runtime/save authority is unchanged, and save schema remains 6.


## Purpose
This matrix defines Unit, SIT, UAT, required evidence, and build gate impact for Sprint 2 through Sprint 4 tickets.

## Sprint 2

| Ticket ID | Ticket name | Source specs | Unit test requirements | SIT requirements | UAT requirements | Required evidence | Build gate impact | Notes |
|---|---|---|---|---|---|---|---|---|
| S2-T00 | Mana Tick and Reserve Pressure Foundation | Docs/01, Docs/02, Docs/17, Docs/29, Docs/30 | Deterministic tick math, reserve guard behavior, pool invariant checks, edge-case underflow prevention | Validate mana outputs consumed correctly by encounter and research pacing flows | Validate player-visible reserve pressure indicators and understandable low-usable-mana behavior | Unit logs, SIT scenario run, UAT notes, invariant assertion report | Blocks build | Foundation contract for all Sprint 2 loop work. |
| S2-T00A | Dungeon Layout and Placement MVP Foundation | Docs/00, Docs/05, Docs/17, MVP scope doc | Placement validation guards, deterministic layout serialization, state transition checks | Validate layout to encounter input projection and save trigger integration | Validate edit flow clarity for room or tile placement and rejection messaging | Unit logs, placement SIT logs, UAT screenshot sequence, save trigger trace | Blocks build | Core input contract for encounter pipeline. |
| S2-T01 | Research Queue State Machine (Single Slot) | Docs/09, Docs/17, Docs/28 | State transition table tests, invalid transition rejection tests, single-slot exclusivity tests | Validate mana to research to pending verification queue state propagation | Validate pending and blocked messaging clarity for player-facing research flow | Unit logs, SIT state trace, UAT capture of pending or blocked states | Blocks build | Sprint 2B heavy dependency with verification queue. |
| S2-T02 | Verification Intent Queue and Idempotent Confirm | Docs/25, Docs/34, Docs/35 | Queue lifecycle transition tests, idempotent confirm tests, duplicate or stale response guards | Validate research completion to queued intent to confirm path with reconnect-safe behavior | Not applicable: primarily system integrity path; player sees resulting status via S2-T07 messaging | Unit logs, SIT reconciliation logs, API contract fixture evidence | Blocks release candidate | UAT N/A because direct acceptance is technical integrity behavior. |
| S2-T03 | Heat State and Risk-Reward Regulation (MVP thresholds) | Docs/02, Docs/30, Docs/36 | Heat threshold boundary tests, deterministic tier transitions, edge-case clamp tests | Validate encounter and loot outputs drive heat transitions that feed mana efficiency | Validate player-visible heat state changes and risk feedback comprehension | Unit logs, SIT flow report, UAT script output with before or after states | Blocks build | Keep tuning within locked MVP constants. |
| S2-T04 | Offline Tick, Save Snapshot, and Return Summary Baseline | Docs/17, Docs/29, Docs/35 | Offline elapsed resolution tests, save schema persistence tests, edge-case elapsed cap tests | Validate save or load with layout, mana, research, verification queue and offline summary inputs | Validate offline return summary clarity and trust messaging on return session | Unit logs, SIT persistence run, UAT summary screenshot and notes | Blocks release candidate | Core trust requirement for idle loop reliability. |
| S2-T05 | Deterministic Encounter Resolution Baseline | Docs/03, Docs/04, Docs/06, Docs/08 | Deterministic seed replay tests, encounter state transition tests, invalid input guard tests | Validate layout to encounter to loot to heat to mana chain integration | Validate player-facing encounter output readability and consistency | Unit logs, SIT chain run artifacts, UAT script evidence | Blocks build | Primary gameplay resolution gate. |
| S2-T06 | Loot Resolution, Inventory Intake, and Content Integrity Guards | Docs/05, Docs/19, Docs/23 | Loot table deterministic roll tests, invalid reference guard tests, inventory state update tests | Validate encounter payload to loot resolver to inventory persistence flow | Validate loot outcome clarity and expected inventory updates from player view | Unit logs, SIT payload and inventory trace, UAT screenshots | Blocks build | Data integrity and reward loop correctness gate. |
| S2-T07 | Trust State Surfacing, Error Messaging, and Localization Keys | Docs/15, Docs/27, Docs/35 | Localization key contract tests, message fallback tests, state label mapping tests | Validate pending or failed verification, low mana, and heat warnings across integrated loop states | Validate first-loop clarity for messaging and next-action guidance | Unit logs, SIT state-messaging map, UAT walkthrough evidence | Blocks release candidate; missing localization keys block build/test | Non-critical copy polish can warn, but missing keys or misleading pending/restricted state messaging blocks promotion. |

## Sprint 3

| Ticket ID | Ticket name | Source specs | Unit test requirements | SIT requirements | UAT requirements | Required evidence | Build gate impact | Notes |
|---|---|---|---|---|---|---|---|---|
| S3-T01 | Save Partition Model and Migration Matrix | Docs/17, Docs/28, Docs/29 | Partition schema tests, migration fixture coverage tests, malformed payload guard tests | Validate end-to-end load or migrate or save path across partitions | Not applicable: internal data reliability item with no direct player-operated flow | Unit logs, SIT migration matrix report, fixture diff artifacts | Blocks build | UAT N/A because acceptance is schema and migration correctness. |
| S3-T02 | Verification Reconciliation Conflict Handling | Docs/25, Docs/34, Docs/37 | Conflict policy unit tests for duplicate, stale, missing ack classes | Validate reconnect reconciliation across queue, save, and telemetry signals | UAT limited: validate understandable player state after reconnect conflict outcomes | Unit logs, SIT reconnect scenario logs, focused UAT notes | Blocks release candidate | High-integrity gate with user trust impact. |
| S3-T03 | CI Content Integrity Gates (Schema + FK + Migration Rules) | Docs/19, Docs/28, Docs/30 | Validator unit tests for schema, FK, migration rule checks | Validate CI pipeline fail-fast behavior and report artifact generation | Not applicable: CI internal tooling item | Unit logs, CI SIT run output, validation report artifact | Blocks build | UAT N/A due to non-player-facing CI infrastructure scope. |
| S3-T04 | Performance/Memory Budget Harness and Regression Gate | Docs/18, Docs/36, Docs/37 | Benchmark harness unit tests, threshold evaluator tests | Validate profile scenario execution and regression gate integration in CI | Not applicable: technical gate, no direct player flow test script | Unit logs, SIT benchmark runs, regression diff artifact | Blocks release candidate | UAT N/A; acceptance handled by perf evidence thresholds. |
| S3-T05 | Release Readiness Checklist and Go or No-Go Dashboard | Docs/33, Docs/34, Docs/37 | Dashboard status evaluator tests and checklist completeness checks | Validate ingest of Unit, SIT, UAT, and build-gate evidence links | Validate stakeholder walkthrough clarity for release decision workflow | Unit logs, SIT evidence ingest run, UAT walkthrough recording or notes | Blocks release candidate | Governance control point for promotion readiness. |

## Sprint 4

| Ticket ID | Ticket name | Source specs | Unit test requirements | SIT requirements | UAT requirements | Required evidence | Build gate impact | Notes |
|---|---|---|---|---|---|---|---|---|
| S4-T01 | Telemetry-Driven Core Loop Balance Pass | Docs/14, Docs/16, Docs/18, Docs/21 | Determinism regression tests after parameter changes, bounded-value validation tests | Validate telemetry pipeline to tuning table to runtime resolver consistency | Validate first-loop progression feel and pacing clarity using UAT script | Unit logs, SIT telemetry or resolver trace, UAT comparison notes | Blocks release candidate | Tuning only; no new systems or scope. |
| S4-T02 | First-Session Onboarding Clarity Pass | Docs/15, Docs/20, Docs/35 | Trigger-state unit tests, fallback message unit tests | Validate onboarding triggers across loop events and restricted states | Validate first-session completion clarity and next-action comprehension | Unit logs, SIT onboarding state trace, UAT first-session run artifacts | Blocks release candidate | Player-facing acceptance critical for MVP polish. |
| S4-T03 | Accessibility and Cognitive Load Evidence Pass | Docs/26, Docs/15, Docs/35 | Readability and label mapping rule tests where automated checks exist | Validate accessibility checklist integration with runtime UI states | Validate accessibility and cognitive load outcomes in guided UAT run | Unit logs, SIT checklist output, UAT accessibility evidence pack | Blocks release candidate | Evidence-first pass; no broad redesign scope added. |
| S4-T04 | MVP Test Override Hooks, Not Player-Facing Events | Docs/32, Docs/00 | Override registry unit tests, unsupported request rejection tests, baseline equivalence tests | Validate override application path does not alter default runtime when disabled | Validate internal tester flow for approved hooks only, if approved for Sprint 4 execution | Unit logs, SIT override enable or disable traces, UAT internal script evidence if applicable | Warning only | Must confirm no live-ops, seasonal, leaderboard, event pass, or social features are introduced. |

## GD65B production spatial content pipeline gate

This focused gate supplements, and does not rewrite, the historical Sprint 2–4 matrix. GD65B0C7 approved the responsibilities. Current implementation includes workload/scalability, deterministic export through PR #182, recoverable publication through PR #183, export invocation and committed outputs through PR #184, inactive loading/composition through PR #185, and the validated build gate through PR #186. Complete EditMode and PlayMode suites passed; PR #186 is merged and GD65B is closed.

| Gate | Unit / EditMode | SIT | UAT | Required fixtures and evidence | Build impact |
|---|---|---|---|---|---|
| Workload limits | Exact five-value deserialization; missing/malformed/nonpositive/overflow rejection; exact/one-over boundaries; lookup/language accounting; stable reasons/order; shared caller limits; source non-mutation | Configuration identity across export, pre-build, and load | N/A while catalog is inactive | Exact/one-over and invalid-limit fixtures; focused and full EditMode XML/summary | Blocks build and GD65B merge |
| Deterministic export | Authoring-package parsing; exact schema and normalized-text validation; duplicate-authority, primary-key, foreign-key, unknown/missing-table/column rejection; canonical ordering; no test-default authority | Row permutations and repeated runs produce byte-identical UTF-8-no-BOM/LF/one-newline files; reparse/revalidate; limits/Bootstrap untouched | N/A | Production and permutation fixtures; command/exit, hashes, diff/no-diff, paths/hashes | Blocks build and GD65B merge |
| Recoverable transaction | Deterministic failure injection and diagnostics | Interrupt before journal and after every staging/backup/journal/install/validate phase; recover complete old/new set; reject mixed set; cleanup only after success | N/A | Journal/recovery and mixed-set fixtures; interruption/recovery logs | Blocks export, build, and GD65B merge |
| Production loading | Language, limits, manifest/content, no-partial-publication boundaries | Explicit catalog/manifest/language collection/limits; exactly one English; append valid test language pack without API change; no Bootstrap fallback | N/A | Valid production set and invalid language/key/version fixtures; load results | Blocks build and GD65B merge |
| Pre-build gate | Shared validation-service and assignment rules | Every supported player-build entry point recovers then validates; invalid/missing/duplicate/mixed input blocks; success never regenerates | N/A | Pre-build pass plus at least one rejection; existing readiness results | Blocks every player build and GD65B merge |
| Scalability contract | Test-only 80 floors, indexes 0–79, current shape/four allowlist refs, complete lookup; test-only 4,096 pass/4,097 fail | Same limits validate/canonicalize deterministically; lower caller limit returns `WorkloadExceeded`; no code/schema/enum/save/API change | N/A; not production content or a gameplay flow | 80-floor and 4,097-tile test-only fixtures; focused/full results | Blocks GD65B merge if absent/failing or fixed scale assumptions are required |

The two GD65B1 fixtures are an initial implementation only and do not mark all six responsibilities complete. Evidence belongs at `docs/testing/evidence/gd65b/`. QA owns the matrix/checklist/verdict/release gate; Engineering owns tests, deterministic seams/helpers, and failure injection; Data / Content Pipeline owns production/configuration/localization/manifest fixtures and canonical expectations; the primary developer/project owner runs complete Unity validation and supplies final evidence. Missing or ambiguous evidence, a failed required test, different limits, nondeterminism, mixed publication, ignored recovery, partial data, Bootstrap/test fallback, generated limits, bypassable build validation, or code/schema changes required by the 80-floor fixture is build-blocking. UAT is not applicable to this inactive technical pipeline and passing it proves neither fun nor mobile/runtime rendering performance.

**Historical PR #184–#185 status, reconciled by PR #186:** PR #184 / GD65B3B merged at `04515d5c7c5a35d869bb725cd76d2a7c317403ee`, and PR #185 subsequently completed strict inactive loading and explicit composition. The former PR #184 evidence gaps are fully reconciled in `docs/testing/evidence/gd65b/validation-evidence.md`. GD65B5 implementation and required validation passed at `c5eefae61e9bf3b7bf0a200e343f383f0122743b`; PR #186 is merged; GD65B is closed and GD66 was subsequently approved in merged PR #187. The catalog remains inactive, runtime/save authority is unchanged, and save schema remains 6.


**GD65B2A evidence boundary:** this packet is documentation-only and adds no Unity evidence or seventh named test class. The six approved identifiable responsibilities remain `ProductionSpatialContentWorkloadLimitTests`, `ProductionSpatialContentExportTests`, `ProductionSpatialContentRecoveryTests`, `ProductionSpatialContentLoadingTests`, `ProductionSpatialContentBuildGateTests`, and `ProductionSpatialContentScalabilityTests`. Future deterministic-export responsibilities include authoring-package parsing, exact schema validation, duplicate-authority rejection, foreign-key validation, normalized-text enforcement, and row-permutation byte determinism. Existing GD65B1 workload/scalability evidence is preserved. UAT remains not applicable while the catalog is inactive.

## GD65B2B source-package coverage

`ProductionSpatialContentExportTests` now begins its approved deterministic-export responsibility with Unit coverage for strict manifest/schema/CSV parsing and SIT coverage for package-to-detached-catalog/string-table projection through existing workload preflight, validation, and canonicalization. UAT is not applicable because GD65B2B does not publish or activate runtime content. Unity compilation and EditMode execution remain owner-run merge evidence.

## Historical GD65B deterministic generated-set coverage

`ProductionSpatialContentExportTests` retains deterministic-export ownership and adds Unit coverage for exact paths, normalized deterministic bytes, strict JSON structure, defensive copies, atomic failure diagnostics, workload/canonical validation, and byte-stable reserialization. SIT covers authoring projection through in-memory build and runtime-safe reparse. UAT, PlayMode, standalone-build, and save reopen testing are not applicable because the committed content remains inactive and is not published to runtime services. Unity compilation, focused EditMode execution, and the complete EditMode suite remain owner-run merge evidence when Unity is unavailable to Codex. GD65B3A adds focused recoverable publication/recovery tests; owner-run Unity evidence remains pending.


### Historical GD65B3A recoverable publication status

PR #182 completed deterministic in-memory construction and strict complete-set reparse of the three approved generated outputs. GD65B3A adds the editor-only recoverable publication and recovery core with a strict durable journal, canonical three-file installation, complete-set readback validation, and deterministic failure injection. Editor-menu and Unity command-line entry points remain absent, and generated production files remain uncommitted. Production loading, `GameRoot` assignment, pre-build gating, and complete evidence remain incomplete. Save schema remains 6, the production spatial catalog remains inactive, runtime/save authority is unchanged, and GD66 remains blocked.

## Historical GD65B3B evidence status

PR #184 owner validation passed 169 of 169 EditMode tests with zero PlayMode failures. The exact final PlayMode count was not retained, and the two PR #184 evidence placeholders remain unverified. PR #185 Unity evidence remains pending.
